export interface Veterinaria {
    id: number;
    mascotas_at: number,
    cant_duenos: number,
    cant_person:number,
    nombre: string;
    direccion: string;
    c_X: string; // Asumiendo que estas son las coordenadas
    c_Y: string;
}